#include <iostream>

int main() {
    int a, b, c;

    std::cin >> a;
    std::cin >> b;
    std::cin >> c;

    std::cout << a * 0.6 + b * 0.2 + c * 0.2;

    system("pause");
    return 0;
}